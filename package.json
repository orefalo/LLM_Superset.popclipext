{
  "name": "LLM_Superset.popclipext",
  "version": "2.0.0",
  "description": "PopClip LLM extensions",
  "private": "true",
  "scripts": {
    "check": "tsc --noEmit",
    "format": "biome format --write",
    "build": "esbuild src/Config.ts --bundle --platform=node --packages=external --target=node10.4 --outfile=src/Config.js",
    "bundle": "pnpm build && ./bundle.sh",
    "deploy": "pnpm bundle && open LLM_Superset.popclipext",
    "test": "ts-node src/customUrlParser.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "axios": "1.13.2",
    "typescript": "5.9.3",
    "@biomejs/biome": "2.3.10",
    "@popclip/types": "1.4892.0",
    "esbuild": "0.27.2",
    "@types/node": "25.0.3"
  }
}